'use strict';

describe('App Actions', function() {
  var action;

  beforeEach(function() {
    action = require('actions/App.js');
  });

  it('should be defined', function() {
    expect(action).toBeDefined();
  });
});
